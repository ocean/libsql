[package]
name = "test_serialization"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3.3"
postcard = { version = "1", features = ["use-std"] }
